{% extends 'base.html.twig' %}
{% block title%}Modifier la catégorie
{% endblock %}
{% block body %}
	<div class="container">
		<div class="form">
			<h1>Modifier la catégorie</h1>
			{{ form_start(form) }}
			{{ form_widget(form) }}
			<button type='submit' class='btn btn-success'>Enregistrer</button>
			{{ form_end(form) }}
			<a href="{{path('app_category')}}">Retour à la liste</a>
		</div>
	</div>

{% endblock %}
